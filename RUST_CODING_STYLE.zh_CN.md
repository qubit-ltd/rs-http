# Rust 编码风格

[English Version](RUST_CODING_STYLE.md)

本文档说明向 `qubit-http` 贡献 Rust 代码时需要遵循的编码风格。
它补充 `rustfmt`、`clippy` 和当前代码库已经形成的约定。

## 基本原则

- 遵循现有模块布局、命名方式、错误模型和文档注释风格。
- 优先编写清晰、可读、可验证的代码，不为了抽象而抽象。
- 保持变更聚焦。除非当前改动确实需要，否则避免大范围重构。
- 公共行为变化应包含测试，并同步更新面向用户的文档。
- 不要为了方便直接暴露完整 `reqwest` API；本 crate 的定位是稳定的 HTTP 基础设施层。

## 格式化与工具

提交 pull request 前，请尽量在本地运行相关检查：

```bash
cargo fmt
cargo clippy --all-targets --all-features
cargo test
```

如果某个检查无法在你的环境中运行，请在 pull request 中说明。

## 模块组织

- `src/` 目录只放生产源码。不要在 `src/` 中放测试代码，也不要在源码文件中新增
  `#[cfg(test)]` 测试模块。
- 所有测试代码都应放在 `tests/` 目录。共享测试 fixture 和集成测试辅助代码可以放在
  `tests/common` 或其他命名清晰的测试专用模块中。
- 源文件使用 `snake_case.rs` 命名。
- 原则上，每个公开的 `struct`、`trait` 或 type alias 单独放在一个源文件中。
  源文件名必须是公开类型名的小写下划线形式，例如 `HttpClientOptions`
  对应 `http_client_options.rs`。
- 一些只用于内部实现、语义密切相关的数据类型，可以放在同一个源文件中；
  如果强行拆分会降低可读性，则不必拆分。
- 每个源文件都应在 `tests/` 目录下有一个对应的单元测试文件，且测试文件路径与
  被测文件在 `src/` 下的相对路径一致。测试文件名由被测文件名去掉 `.rs`
  后追加 `_tests.rs` 构成；例如 `src/options/proxy_options.rs` 对应
  `tests/options/proxy_options_tests.rs`。
- `mod.rs` 导出方式应与现有源码和测试布局保持一致。
- 可见性应尽量收窄：优先私有，其次 `pub(crate)`，只有确实需要进入 crate 公共 API 时才使用 `pub`。
- 新增公共导出前，请确认它确实属于预期的 crate API。

## API 设计

- 字符串输入优先使用 `&str`，除非确实需要取得所有权。
- 集合输入优先使用切片（`&[T]`），不要使用 `&Vec<T>`。
- 在热点路径中避免不必要的 `String`、`Vec`、`clone` 和分配。
- 当类型包含多个可选设置时，优先使用 builder 风格 API。
- 当状态分支具有明确语义时，优先使用枚举表达，避免用多个布尔值拼出状态机。
- 异步 API 的返回类型应清晰，优先返回 `Result<T, HttpError>` 或项目内其他具体错误类型。
- 不要在异步代码中执行阻塞 I/O。
- 共享状态的临界区应尽量短，并保持容易推理。

## 错误处理

- 公共 API 应返回具体错误类型。本 crate 中优先使用 `HttpError`、`HttpConfigError` 或 `HttpResult<T>`。
- 错误信息应保留足够上下文，例如 HTTP 方法、URL、状态码、超时阶段、配置 key 或重试状态。
- 生产代码中避免使用 `unwrap()` 和 `expect()`。如果因为不变量必须使用 `expect()`，错误信息必须具体。
- 使用 `HttpErrorKind` 和 `RetryHint` 时应保持重试语义一致。
- 后端错误应通过现有错误映射代码转换，不要引入零散的临时转换逻辑。

## 文档注释

项目中的 Rust 文档注释使用英文。

- 模块级文档使用 `//!`。
- 公共类型和非平凡公共方法应说明用途、关键约束和正确使用方式。
- 函数和方法文档应覆盖参数、返回值、错误情况和重要副作用。
- `Result` 文档应说明哪些条件会返回 `Err`，以及错误中携带什么上下文。
- `Option` 文档在语义不明显时应说明 `Some` 和 `None` 分别代表什么。
- 异步 I/O、取消、重试、日志副作用和流消费行为应明确说明。
- 示例应聚焦，并尽量与 doctest 预期兼容。

## 测试

- 测试放在 `tests/` 目录。
- 测试文件使用 `_tests.rs` 后缀。
- 测试函数使用 `test_{method_or_behavior}_{scenario}` 命名模式。
- 覆盖正常路径、错误路径、边界条件和回归场景。
- 优先使用精确断言，不要只验证“失败了”。
- 测试不应依赖外部网络或不稳定外部服务。
- 测试中优先使用带有有用信息的 `expect()`，避免随手 `unwrap()`。

## 日志与安全

- 遵守敏感请求头脱敏规则。
- 不要记录密钥、完整凭据或原始二进制请求体/响应体。
- 请求体和响应体预览必须有长度上限。
- 修改日志行为时，应覆盖请求头脱敏和 body 截断场景。

## 性能

- 避免不必要的响应体缓冲；除非行为明确需要，否则响应体应保持惰性读取。
- 已知集合大小时优先预分配。
- 除非重试、日志或所有权边界确实需要，否则避免克隆请求体或响应体。
- 新增流式行为时，应考虑背压和取消。

## 文档更新

公共行为发生变化时，请更新相关文档：

- `README.md` 和 `README.zh_CN.md`：项目定位和入口说明。
- `doc/user_guide.en.md` 和 `doc/user_guide.zh_CN.md`：使用细节。
- Rustdoc 注释：API 契约和边界情况。
